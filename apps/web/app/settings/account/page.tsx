import { PlaceholderPage } from "@/components/placeholder-page";

export default function AccountSettingsPage() {
  return (
    <PlaceholderPage
      title="Account"
      description="Billing, business info, and team members."
      nextStep="Stripe Billing portal + Clerk org settings."
    />
  );
}
